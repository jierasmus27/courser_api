require 'spec_helper'

describe CourserApi do
  it 'has a version number' do
    expect(CourserApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
